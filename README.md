# Baby Feed Trends

Public static dashboard (GitHub Pages).

This repository contains **only** the generated HTML and assets.
No LLIF API credentials, refresh scripts, or raw export dumps live here.

Source of truth / generators remain in a private repo.
